<div class="header-autors">
  <h1 class="text-center">List of all authors</h1>
</div>

<img style="display: none;" src="https://loading.io/spinners/balls/lg.circle-slack-loading-icon.gif"/>

<div class="container autors">
  <% @autors.each do |autor| %>
  <!-- kazdy row ma zawsze 12 kolumn -->
    <%= render 'autor', autor: autor %>
  <% end %>
</div>

<!-- <div class="text-center"><%= link_to 'Add new author', new_autor_path, role: 'button', class:'btn btn-outline-success' %></div> -->
<p class="new-author-p text-center"> You can add new author below: </p>

<!-- forumlarze metodami pomocniczymu -->
<!--<%=  form_for @autor, remote: true, html: { class: "new-autor" }  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :surname %>
  <%= f.text_field :surname %>
  <%= f.label :age %>
  <%= f.number_field :age %>
  <%= f.submit 'Save' %>
<% end %> -->

<%=  form_for @autor, remote: true, html: { class: "new-autor" }  do |f| %>
  <div class="form-group">
    <%= f.label :name, class: "control-label" %>
    <%= f.text_field :name, class: "form-control col-md-2 col-centered text-center", placeholder: "Enter your name"%>
    <small id="nameHelpBlock" class="form-text text-muted">
      Your name must be at least 2 characters long.
    </small>
  </div>

  <div class="form-group">
    <%= f.label :surname, class: "control-label" %>
    <%= f.text_field :surname, class: "form-control col-md-2 col-centered text-center", placeholder: "Enter your surname" %>
    <small id="surnameHelpBlock" class="form-text text-muted">
      also at least 2 characters
    </small>
  </div>

  <div class="form-group">
    <%= f.label :age, class: "control-label" %>
    <%= f.number_field :age, class: "form-control col-md-1 col-centered text-center"%>
    <small id="ageHelpBlock" class="form-text text-muted">
      You can lie :)
    </small>
  </div>
  <hr />
  <%= f.submit 'save', role: 'button', class:'btn btn-outline-success' %>
<% end %>


<% if @autor.errors.any? %>
  <ul>
    <% @autor.errors.full_messages.each do |msg| %>
      <li>
        <%= msg %>
      </li>
    <% end %>
  </ul>
<% end %>
